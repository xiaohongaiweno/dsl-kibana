# 知乎长文版推广稿：我把 Kibana Console 的 DSL 能力单独剥离出来了，方便中大型公司集成到自有平台

> 项目地址: https://github.com/xiaohongaiweno/dsl-kibana

## 备选标题

- 我把 Kibana Console 的 DSL 能力单独剥离出来了，方便中大型公司集成到自有平台
- 中大型公司接入 Kibana 成本太高？我做了一个可独立集成的 Elasticsearch DSL 控制台
- 为什么我没有直接集成 Kibana，而是单独做了一个 DSL 控制台
- 一个更适合企业内部平台集成的 Elasticsearch DSL 控制台开源项目
- 我开源了一个 Kibana 风格的 Elasticsearch DSL 控制台，适合二次开发

## 一句话摘要

`dsl-kibana` 是一个独立运行的 Elasticsearch DSL 控制台项目，复刻了 Kibana Console 的核心体验，支持多请求块编辑执行、Kibana 风格自动补全、`ES6`/`ES7`/`ES8` 规则切换，并将最有价值的 DSL 能力从完整 Kibana 中剥离出来，更适合中大型公司集成到自己的研发平台、运维平台和数据平台中。

## 导语

最近把一个自己实际会用到的小工具开源了，名字叫 `dsl-kibana`。

它的初衷并不是再做一个“大而全”的 Elasticsearch 平台，也不是简单复刻一个完整 Kibana，而是想解决一个我觉得很多团队都会遇到的现实问题：

很多中大型公司其实已经有自己的开发框架、统一门户、权限体系、审计体系和内部平台了。

这个时候，如果团队只是想在自己的平台里提供一个 Elasticsearch DSL 调试能力，最直接的思路通常是两种：

1. 直接跳到完整 Kibana 去用
2. 想办法把 Kibana 整体接入自己的平台

但这两条路，在企业实际环境里往往都不算特别理想。

第一种方式的问题是，它更适合“独立使用”，但不一定适合“平台集成”。

第二种方式的问题则更明显。因为企业并不是缺一个页面，而是缺一个能够按自己技术体系、权限体系和安全规范嵌入进去的能力模块。

如果为了一个 DSL 控制台能力就整体接入 Kibana，很多时候会出现下面这些情况：

- 接入和改造工作量不小
- 跟公司自有前端框架不一致
- 跟统一门户、菜单、权限体系的融合成本高
- 只需要 DSL 能力，却引入了一整套不一定都用得上的系统能力
- 还要承担额外的安全治理和维护压力

所以我后来换了一个思路：

既然团队真正高频使用的是 Kibana Console 那部分 DSL 能力，那为什么不把这部分能力单独剥离出来，做成一个更轻、更聚焦、更适合二次集成的项目？

`dsl-kibana` 就是基于这个思路做出来的。

## 为什么不直接集成完整 Kibana

这个问题我觉得值得单独展开讲，因为它也是这个项目最核心的定位。

很多时候，技术选型的问题不是“能不能做”，而是“值不值得这样做”。

如果一个团队本身没有内部平台，也没有统一技术框架，那直接使用 Kibana 当然没问题，甚至是最自然的选择。

但对很多中大型公司来说，情况并不是这样。

它们往往已经有：

- 自研前端框架
- 自研中后台体系
- 统一 SSO 登录
- 统一权限模型
- 统一菜单和导航体系
- 审计、风控和访问隔离要求

这时如果你要把完整 Kibana 接进来，问题通常不只是在“页面能不能打开”，而在于下面这些成本会一起出现。

### 1. 接入成本高

你可能需要考虑：

- 怎么嵌入现有平台
- 怎么处理路由和菜单
- 怎么处理登录态打通
- 怎么跟现有权限体系映射
- 怎么满足企业内部的前端规范

这时候，集成的已经不是一个“查询能力”，而是一整个外部系统。

### 2. 性价比不高

很多团队真正想要的，其实只有这些：

- 一个可以写 DSL 的编辑器
- 一套尽量好用的自动补全
- 一个可以执行请求的入口
- 一个结果展示面板

但如果为了这几个核心能力引入完整 Kibana，投入和收益经常不成比例。

### 3. 安全风险和治理成本更重

企业内部平台集成最怕的一点，就是把本来只想开放的一小块能力，最后变成需要治理一整套系统边界。

比如：

- 权限隔离怎么做
- 哪些人可以访问
- 请求能力如何审计
- 页面如何暴露在公司网络中
- 平台安全面如何收敛

这些事情并不一定是 Kibana 本身做不到，而是你为了一个 DSL 控制台能力去承担整套治理成本，往往不够划算。

## 这个项目到底做了什么

`dsl-kibana` 的目标很明确：

把 Kibana Console 中最有价值、最有生产力的 DSL 能力单独剥离出来，做成一个更轻量、可独立运行、可继续二次开发、也更容易嵌入企业自有平台的项目。

它当前已经具备这些核心能力：

### 1. 多请求块编辑与执行

可以在同一个编辑器中连续写多段 Elasticsearch 请求。

每个请求块左侧都有单独的运行按钮，可以按块执行，而不是一次性把整份内容全部跑掉。

这个交互非常适合日常调试，也很适合企业内部工具场景，因为一个页面上往往会保留多段常用请求模板。

### 2. 接近 Kibana Console 的自动补全体验

项目内置了 Kibana 风格的自动补全能力，覆盖：

- HTTP 方法补全
- 接口路径补全
- URL 查询参数补全
- URL 参数值补全
- JSON Body 字段补全

这意味着它不是一个“只能发请求的文本框”，而是一个真正有生产力的 DSL 编写工具。

### 3. 支持 ES6 / ES7 / ES8 规则切换

对于很多企业环境来说，Elasticsearch 版本并不一定完全统一。

有些历史系统还在旧版本，有些新业务已经迁移到新版本，这会导致接口规则、路径模式甚至部分历史类型兼容行为都存在差异。

`dsl-kibana` 已经支持 `ES6`、`ES7`、`ES8` 三套规则切换，这一点对实际落地很有帮助。

### 4. 直接请求本地 Elasticsearch

项目默认向下面地址发请求：

```text
http://localhost:9200
```

对开发调试来说，这个路径足够直接。

当然，如果企业内部后续需要接自己的网关、代理或统一服务出口，也可以在此基础上继续改造。

### 5. 更适合二次开发和嵌入式集成

这一点其实是我最想强调的。

因为这个项目不是为了把所有功能都做成最终形态，而是希望它可以成为一个“可集成的 DSL 能力模块”。

也就是说，你完全可以把它当成：

- 企业研发平台里的一个 ES 查询页
- 数据平台里的一个 DSL 调试面板
- 运维平台里的一个内部控制台
- 搜索团队内部的一个轻量工具页

而不需要把完整 Kibana 整体塞进你的平台体系里。

## 为什么我觉得它更适合中大型公司

这个项目如果只讲“好不好用”，其实很多人都会觉得它是一个不错的小工具。

但如果从“为什么值得关注”这个角度，我认为它真正的差异化在企业场景。

中大型公司的典型特点是：

- 已有成熟平台
- 已有统一权限
- 已有自研技术框架
- 更关注安全边界
- 更关注投入产出比

所以它们的诉求往往不是“再上一个完整系统”，而是“把一个高频能力以更低成本接进现有系统”。

`dsl-kibana` 对应的正是这个需求。

它不是想替代 Kibana 的所有能力，而是把 Kibana Console 里最常用、最容易产生业务价值的那部分体验抽取出来。

从这个角度看，它更像是一个能力模块，而不是一个平台替代品。

## 这类项目最适合落地在哪些地方

如果你在公司内部做平台建设，我觉得下面几个方向会比较适合：

### 1. 研发平台

给后端、搜索、推荐、数据开发同学提供一个统一的 DSL 调试入口。

### 2. 运维平台

在受控权限下提供 ES 查询、排障和索引检查能力。

### 3. 数据平台

作为某些数据调试、索引验证和查询实验的辅助工具页。

### 4. 搜索团队内部平台

把查询调试能力直接嵌进团队自己的工作台里，避免多人在多个外部工具之间切换。

## 技术上它为什么适合继续扩展

我在做这个项目时，也比较注意不要把所有逻辑都糊在一起。

目前它的核心结构大致是这样的：

- `src/App.vue`: 页面与主流程控制
- `src/kibanaConsoleParser.js`: 多请求块解析
- `src/requestExecution.js`: 请求执行与取消控制
- `src/kibanaConsoleAutocomplete.js`: 编辑器补全适配层
- `src/kibanaConsoleAutocompleteCore.js`: 补全规则核心算法

这套结构的好处是比较容易继续沿着企业需求做扩展，比如：

- 改 UI 风格以适配公司设计系统
- 对接内部认证和权限体系
- 在请求链路前增加代理网关
- 做操作审计
- 增加企业自定义 API 规则和补全规则
- 封装成内部平台组件

换句话说，它更像一个“可继续长出来的基础能力”，而不是一个封死的页面。

## 适合哪些人关注这个项目

我觉得主要有三类人：

### 1. 平时就会写 Elasticsearch DSL 的工程师

如果你经常需要调试查询、验证语句、排查索引问题，那它会是一个比较顺手的工具。

### 2. 在做企业内部平台建设的团队

如果你们公司已经有自己的研发平台、运维平台或数据平台，但又需要一个 ES DSL 控制台能力，这个项目会比较值得参考。

### 3. 想研究 Kibana Console 类交互实现的人

如果你对编辑器交互、请求块解析、自动补全实现、规则匹配这类问题感兴趣，这个项目也比较适合作为学习入口。

## 一个简单的使用示例

项目默认请求本地 Elasticsearch：

```text
http://localhost:9200
```

启动后可以直接输入：

```http
GET /_search
{
  "query": {
    "match_all": {}
  }
}
```

然后：

- 点击请求块左侧 `▶` 执行
- 或者使用 `Ctrl + Enter`
- 右侧面板查看结果、状态码和耗时

如果你在一个文档里写了多段请求，也可以分别按块执行。

## 这个项目不是为了替代 Kibana，而是为了更合理地使用 Kibana 的能力

这一点我想单独说一下。

我并不认为这个项目的价值在于“替代 Kibana”。

Kibana 仍然是一个非常成熟、非常完整的平台。

`dsl-kibana` 更像是在回答另一个问题：

当企业真正只需要 Kibana Console 里那一部分 DSL 能力时，有没有一种方式可以让它更轻量、更可控、更容易集成到自己的平台里？

我觉得这个项目给出的答案是：可以，而且值得做。

## 开源地址

项目已经放到 GitHub：

https://github.com/xiaohongaiweno/dsl-kibana

如果你对下面这些方向感兴趣，欢迎交流：

- Elasticsearch 工具平台
- 企业内部控制台建设
- DSL 编辑器与自动补全
- Kibana Console 类体验复刻
- 企业内部平台二次集成

## 结尾

如果你也遇到过下面这些问题：

- 公司已经有自己的平台，但缺一个顺手的 ES DSL 控制台
- 不想为了一个查询能力整体引入 Kibana
- 想保留 Kibana Console 的核心体验，又希望更容易二次开发
- 想做一个更适合企业内部落地的 Elasticsearch 工具页

那这个项目也许对你会有一些参考价值。

如果你觉得这个方向有意义，欢迎到 GitHub 看看，也欢迎给项目点一个 Star。

项目地址：

https://github.com/xiaohongaiweno/dsl-kibana

## 推荐标签

- Elasticsearch
- Kibana
- 前端开发
- 企业平台
- 开源项目
- 工具开发
- 搜索技术
- 中后台系统
