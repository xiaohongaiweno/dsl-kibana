# dsl-kibana 使用说明书

> 项目地址: https://github.com/xiaohongaiweno/dsl-kibana

## 1. 项目简介

`dsl-kibana` 是一个可独立运行的 Elasticsearch DSL 控制台工具，目标是复刻 Kibana Console 的核心使用体验，并把最常用的查询编写、自动补全、请求执行和结果查看能力直接放到一个轻量级前端项目里。

它更重要的价值在于，把 Kibana 中最有生产力的 DSL 能力单独剥离出来，方便中大型公司集成到自己的研发平台、运维平台、数据平台或搜索平台中。

它适合下面这几类人使用：

- Elasticsearch 开发者
- 搜索平台研发工程师
- 日常写 DSL 的后端工程师
- 需要快速验证查询语句的数据开发同学
- 想研究 Kibana Console 自动补全实现思路的前端或工具链开发者

相比直接进入完整的 Kibana 环境，`dsl-kibana` 更轻、更聚焦，也更适合做本地调试、企业内平台集成、二次开发和源码学习。

## 2. 这个项目能解决什么问题

日常使用 Elasticsearch 时，很多人都会遇到下面这些问题：

- 想快速试一段 DSL，却不想启动完整的 Kibana
- 想在一个编辑器里维护多段请求并逐条执行
- 写 DSL 时容易记错接口路径、查询参数和 JSON 结构
- 不同 Elasticsearch 版本的接口规则存在差异，切换时容易混淆
- 想基于 Kibana Console 体验做自己的工具，但原始实现体量较大，不容易改
- 中大型公司通常已经有自己的开发框架、统一门户和内部平台，直接把完整 Kibana 集成进去改造成本很高
- 为了接入一个 DSL 调试能力而引入完整 Kibana，性价比偏低，维护成本也不划算
- 直接嵌入或暴露 Kibana 还需要额外承担权限隔离、访问控制和平台安全面的风险

`dsl-kibana` 就是围绕这些实际问题设计的。它把“写请求、补全、执行、看结果”这条链路收敛到一个独立项目中，开箱即可使用，同时也让企业能够只集成自己真正需要的 DSL 能力，而不是整套 Kibana。

## 3. 核心特性

### 3.1 多请求块编辑与执行

在同一个编辑器中可以连续书写多段 Elasticsearch 请求，每个请求块左侧都有独立的运行按钮，点击即可单独执行当前块。

你也可以把光标放在某一段请求中，直接按 `Ctrl + Enter` 执行当前请求块。

### 3.2 接近 Kibana Console 的自动补全体验

项目内置了 Kibana Console 风格的补全能力，支持：

- HTTP 方法补全
- 接口路径补全
- URL 查询参数补全
- URL 参数值补全
- JSON Body 字段补全

这意味着你在编写 DSL 时，不需要频繁切换文档查接口，输入效率会明显更高。

### 3.3 支持 ES6 / ES7 / ES8 版本规则切换

页面顶部可以直接切换 `ES6`、`ES7`、`ES8` 三套规则。对于不同版本下路径、类型映射和接口模式差异，这个项目已经做了兼容处理，方便你在多版本环境中调试和验证请求。

### 3.4 直接请求本地 Elasticsearch

默认会将请求发送到：

```text
http://localhost:9200
```

这使得本地联调非常直接。启动 Elasticsearch 后，打开页面就可以开始写 DSL 并查看响应结果、状态码和耗时。

### 3.5 更适合二次开发

项目将核心能力拆成了几块：

- `App.vue`: 页面与主流程控制
- `kibanaConsoleParser.js`: 多请求块解析
- `requestExecution.js`: 请求执行与取消控制
- `kibanaConsoleAutocomplete.js`: 编辑器补全适配层
- `kibanaConsoleAutocompleteCore.js`: 补全规则核心算法

这意味着你可以把项目当作一个“可集成的 DSL 能力模块”来看，而不只是一个独立页面：

- 不需要把完整 Kibana 作为一个重量级系统接入公司平台
- 不需要为适配公司自有框架付出过大的改造成本
- 不需要为了一个 DSL 控制台能力承担整套 Kibana 的接入复杂度
- 更容易按企业自己的权限体系、菜单体系和页面风格做二次封装
- 更适合嵌入中大型公司现有的内部平台体系

如果你想基于它继续扩展：

- 企业内部 DSL 调试平台
- ES 教学演示工具
- 面向运维或数据团队的查询面板
- 定制化 Elasticsearch Console

这套结构会比直接改完整 Kibana 更轻松，尤其适合已经拥有自研中后台框架和统一技术底座的公司。

## 4. 适用场景

`dsl-kibana` 特别适合这些场景：

- 本地快速调试 Elasticsearch DSL
- 演示 ES 查询语句的教学过程
- 对比不同 ES 版本下的请求规则
- 为团队搭建轻量级内部查询控制台
- 集成到公司已有研发平台、运维平台或数据平台中
- 在企业自有权限体系下提供可控的 DSL 调试能力
- 学习 Kibana Console 自动补全与编辑器交互实现
- 作为前端工具项目进行二次开发

## 5. 安装与启动

### 5.1 环境要求

项目当前要求：

- Node.js `20.14.0`

仓库中已经提供：

- `.nvmrc`
- `.node-version`

推荐直接使用 `nvm` 切换到对应版本。

### 5.2 克隆项目

```bash
git clone https://github.com/xiaohongaiweno/dsl-kibana.git
cd dsl-kibana/dsl-kibana
```

### 5.3 安装依赖

```bash
nvm use
npm install
```

### 5.4 启动开发环境

```bash
npm run dev
```

启动后按 Vite 输出地址在浏览器中打开即可。

### 5.5 构建生产版本

```bash
npm run build
```

### 5.6 运行测试

```bash
npm test
```

## 6. 快速上手

### 6.1 启动本地 Elasticsearch

请先确保本地 Elasticsearch 已经运行，并能够通过下面地址访问：

```text
http://localhost:9200
```

### 6.2 打开页面后输入 DSL

例如可以直接输入：

```http
GET /_search
{
  "query": {
    "match_all": {}
  }
}
```

然后点击请求块左侧的 `▶` 按钮，或者按 `Ctrl + Enter` 执行。

### 6.3 查看响应结果

执行后，右侧面板会展示：

- 返回结果
- HTTP 状态码
- 请求耗时
- 错误信息

### 6.4 格式化当前请求

点击 `JSON格式化` 按钮，可以对当前请求块进行局部格式化，不会影响文档中的其他请求块。

## 7. 典型使用示例

### 7.1 查询全部文档

```http
GET /_search
{
  "query": {
    "match_all": {}
  }
}
```

### 7.2 指定索引查询

```http
GET /my_index/_search
{
  "query": {
    "match": {
      "title": "dsl"
    }
  }
}
```

### 7.3 一份文档里写多个请求块

```http
GET /_cat/indices?v=true

GET /my_index/_search
{
  "size": 5,
  "query": {
    "match_all": {}
  }
}
```

你可以分别执行每一块请求，这一点非常适合日常调试和问题复现。

## 8. 为什么值得关注这个项目

如果你是 Elasticsearch 生态的使用者，这个项目的价值主要在于：

- 它把 Kibana Console 的核心体验独立出来了
- 它不是大而全后台，而是一个可以直接改、直接学、直接嵌入的工具项目
- 它已经内置 ES6/ES7/ES8 规则切换能力
- 它保留了多请求块执行这一类高频生产力功能
- 它把补全逻辑拆成了适配层和核心层，源码结构清晰
- 它非常适合中大型公司接入自有平台，避免为了 DSL 能力而整体引入 Kibana
- 它能降低平台集成成本，同时减少直接暴露 Kibana 带来的安全顾虑

如果你是开源爱好者或工程实践分享作者，这也是一个很适合写技术解析文章的项目，因为它同时具备：

- 实用性
- 交互性
- 工具属性
- 源码可读性
- 明确的二次开发空间

## 9. 与 Kibana Console 的关系

这个项目并不是简单包装一个外部页面，而是围绕 Kibana Console 核心体验进行独立实现。项目运行时依赖的是仓库内已经生成好的补全元数据文件，不需要在本地额外依赖一套 `kibana-7.6.0` 源码目录才能工作。

这件事对于企业场景尤其重要。很多中大型公司本身已经有统一研发框架、统一登录体系、统一权限平台和自有门户系统。如果直接集成完整 Kibana，往往会遇到下面这些现实问题：

- 接入链路长，改造点多
- 与公司自有前端框架和平台规范不一致
- 只想要 DSL 能力，却要承担整套 Kibana 的集成和维护成本
- 权限、菜单、审计、访问控制等安全治理成本更高

`dsl-kibana` 的价值就在于，它把最核心的 DSL 能力从 Kibana 体验中剥离出来，让团队可以更低成本地集成到自己的平台里。

这意味着：

- 仓库更轻量
- 运行更直接
- 更适合作为独立前端项目维护
- 更方便做功能定制和学习研究

## 10. 推荐传播角度

如果你准备把这个项目发布到公众号、知乎、掘金或技术社群，推荐从下面几个角度切入：

### 10.1 工具价值角度

标题示例：

- 我把 Kibana Console 的核心体验单独做成了一个轻量工具
- 一个更轻量的 Elasticsearch DSL 控制台，支持 ES6/ES7/ES8 自动补全
- 写 Elasticsearch DSL 不想开完整 Kibana？试试这个开源项目
- 中大型公司接入 Kibana 成本太高？我把 DSL 能力单独剥离出来了

### 10.2 技术实现角度

标题示例：

- 我做了一个支持 Kibana 风格自动补全的 Elasticsearch DSL 编辑器
- 如何用 Vue 2 + CodeMirror 6 实现一个类 Kibana Console 的 DSL 控制台
- 从源码角度拆解一个 Elasticsearch DSL 控制台的自动补全设计

### 10.3 开源推广角度

标题示例：

- 开源一个 Elasticsearch DSL 调试工具，欢迎试用和 Star
- 这个项目把 Kibana Console 的核心能力拆出来了，适合二次开发
- 一个适合学习和改造的 Elasticsearch DSL 控制台开源项目
- 适合集成到企业自有平台的 Elasticsearch DSL 控制台开源项目

## 11. 可直接复制的项目简介

下面这段适合放在公众号、知乎开头或仓库介绍中：

> `dsl-kibana` 是一个独立运行的 Elasticsearch DSL 控制台项目，复刻了 Kibana Console 的核心使用体验，支持多请求块编辑与执行、Kibana 风格自动补全、ES6/ES7/ES8 规则切换，并可直接请求本地 Elasticsearch。它最大的价值是把 Kibana 中最常用的 DSL 能力单独剥离出来，方便中大型公司集成到自己的研发平台、运维平台或数据平台中，避免为了一个 DSL 控制台能力整体接入 Kibana 所带来的高改造成本、低性价比和额外安全风险。项目基于 Vue 2、Vite 和 CodeMirror 6 构建，适合本地调试、源码学习和二次开发。

## 12. 可直接复制的推广文案

### 12.1 公众号短文案

> 最近把一个自己常用的 Elasticsearch 调试工具开源了，项目名叫 `dsl-kibana`。它复刻了 Kibana Console 的核心体验，但更轻量，支持多 DSL 请求块编辑、自动补全、单块执行、ES6/ES7/ES8 规则切换。对中大型公司来说，这个项目的价值尤其明显，因为很多公司都有自己的开发框架和内部平台，直接集成完整 Kibana 改造成本高、性价比低，还要承担额外的安全风险。`dsl-kibana` 把最核心的 DSL 能力单独剥离出来，更方便二次集成到公司自有平台。如果你平时会写 Elasticsearch DSL，或者正在做企业内部平台建设，欢迎试用，也欢迎到 GitHub 给我一个 Star 支持。  
> 项目地址: https://github.com/xiaohongaiweno/dsl-kibana

### 12.2 知乎风格文案

> 如果你平时经常写 Elasticsearch DSL，但又不想每次都依赖完整的 Kibana 环境，可以看看我开源的这个小工具 `dsl-kibana`。  
> 它提供了接近 Kibana Console 的交互体验，支持多请求块执行、路径和 Body 自动补全、版本规则切换以及本地 ES 直连。  
> 我认为它最适合的一个方向，其实是企业内部平台集成。很多中大型公司已经有自己的开发框架、统一门户和权限系统，如果为了提供一个 DSL 调试能力而整体接入 Kibana，开发工作量大、性价比低，还会引入额外的安全治理成本。  
> `dsl-kibana` 把 Kibana 中最有价值的 DSL 能力剥离出来，更适合按企业自己的平台规范做二次集成。  
> 这个项目比较适合三类人：一类是日常调试 DSL 的工程师，一类是想做 ES 内部工具的团队，一类是想研究 Console 自动补全实现的开发者。  
> GitHub 已开源，欢迎交流，也欢迎 Star：  
> https://github.com/xiaohongaiweno/dsl-kibana

### 12.3 社群转发短句

> 开源了一个 Elasticsearch DSL 控制台工具 `dsl-kibana`，支持多请求块执行、Kibana 风格自动补全、ES6/ES7/ES8 规则切换。它把 Kibana 的核心 DSL 能力单独剥离出来，更适合集成到中大型公司的自有平台，避免完整接入 Kibana 带来的高改造成本和安全压力，欢迎试用和 Star。  
> https://github.com/xiaohongaiweno/dsl-kibana

## 13. 常见问题

### 13.1 这个项目是否依赖完整 Kibana 运行

不依赖。当前项目可以独立运行，补全元数据已经固化在仓库中。

### 13.2 这个项目默认请求哪个 ES 地址

默认请求：

```text
http://localhost:9200
```

### 13.3 是否支持多个请求写在同一编辑器中

支持。并且每个请求块都可以单独执行。

### 13.4 是否适合继续二次开发

非常适合。项目结构比较清晰，编辑器、解析器、请求执行和补全核心已经拆分成独立模块。

### 13.5 为什么不直接集成完整 Kibana

对于中大型公司来说，直接集成完整 Kibana 往往并不是最优解。因为大多数公司已经有自己的开发框架、统一门户、权限体系和安全规范，接入完整 Kibana 通常意味着更大的改造工作量、更低的投入产出比，以及更多安全治理成本。`dsl-kibana` 的定位就是把最常用的 DSL 能力剥离出来，方便你更轻量地集成到公司自有平台。

## 14. Star 支持语

如果这个项目对你有帮助，欢迎到 GitHub 点一个 Star。

你的关注、反馈和 Star，都会让我继续把这个工具打磨得更好。
